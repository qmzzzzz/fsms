# web-admin 前端环境变量示例
# 复制为 .env / .env.local 后按需修改；以 VITE_ 开头的变量才会暴露给前端代码

# 后端 API 基础路径
# - 默认 /api：开发由 Vite 代理转发到后端（见 vite.config.js），生产由 Nginx 反向代理（见 deployment/nginx.conf.example）
# - 仅在需要直连其他后端地址时才改为完整 URL，如 http://localhost:3000/api
VITE_API_BASE_URL=/api

# WebSocket（Socket.IO）连接地址
# - 留空（默认）：使用同源地址 window.location.origin，/socket.io/ 经 Vite（开发）或 Nginx（生产）代理到后端 3000
# - 开发期如需绕过代理直连后端，可设置为 http://localhost:3000
VITE_WS_URL=

# 前端异常与 Web Vitals 上报端点（G-1）
# - 留空（默认）：错误只留档在浏览器 localStorage（fsrbac.errorLog，环形 50 条），不外发
# - 配置后：批量上报到后端 /client-errors（无需认证、自带限流），
#   后端落 winston 日志；已配置 SENTRY_DSN 时异常类条目同步转发 Sentry
# 同源部署（推荐）：
# VITE_ERROR_REPORT_URL=/client-errors
